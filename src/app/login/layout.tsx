import React from "react";

// meta data
export const metadata = {
  title: "Easy Shop | Login",
  description:
    "Easy Shop Login page give you ability to login into this website",
};
const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
